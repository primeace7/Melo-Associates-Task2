/** The id used for the built-in guideline whose content is never shown or editable. */
export const DEFAULT_GUIDELINE_ID = 'default';

/** How many custom guidelines a person can create, on top of Default. */
export const MAX_CUSTOM_GUIDELINES = 3;

/** How many individual rules a single guideline (Default or custom) can hold. */
export const MAX_RULES_PER_GUIDELINE = 10;
